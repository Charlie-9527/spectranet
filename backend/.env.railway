# Railway 环境变量配置模板
# 在 Railway 控制台的 Variables 页面添加这些环境变量

# 数据库 URL（Railway 自动提供 PostgreSQL）
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT 密钥 - 务必生成随机密钥！
# 生成命令: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-random-secret-key-here

# JWT 配置
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# CORS 配置 - 部署后更新为实际的 Vercel URL
ALLOWED_ORIGINS=http://localhost:5173,https://your-vercel-app.vercel.app,https://www.spectranet.com.cn

# 上传配置
UPLOAD_DIR=/app/uploads
MAX_UPLOAD_SIZE=104857600

# 服务器配置
HOST=0.0.0.0
PORT=$PORT
